aiohttp>=3.9.0
aiohttp-socks>=0.8.4
pyyaml>=6.0
requests[socks]>=2.31.0   # only needed if you use the --use-free-list sync fallback path
PySocks>=1.7.1
python-socks[asyncio]>=2.4.3  # required for CONNECT tunnel and WebSocket relay through SOCKS5 proxies
watchfiles>=0.21.0            # optional but recommended: inotify/kqueue-backed config hot-reload (fix #14)
                              # without it the server falls back to polling config.yaml every 5 seconds
